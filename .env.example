# Copy this file to .env and fill in the values. The .env file is gitignored.
# Only non-secret, client-safe values belong here — it is bundled into the app.

# Base URL of the HRIS backend, including the /api/v1 prefix.
API_BASE_URL=https://hris-api.ardentnetworks.com.ph/api/v1
